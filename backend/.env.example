PORT=8787
DB_PATH=./data/notifier.db
LOG_LEVEL=info

# Jeton secret pour accéder à la page d'admin (/admin) qui gère les devices.
# Laisser vide désactive l'interface admin. Génère une valeur longue et aléatoire, ex:
# node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
ADMIN_TOKEN=
